# 概要
サーバとクライアント間で送受信されるバイト列のルールを記述する。

# Client > Server
計1Byte
- byte 0:
  - ユーザの入力を表す。
  - 第0ビットが、RightArrow入力の有無を表す。入力があれば1。
  - 第1ビットが、LeftArrow入力の有無を表す。入力があれば1。

# Server > Client
先頭の制御バイトで、信号の種類(ゲーム状態信号, ゲーム開始信号, ...)を識別する。

## ゲーム状態信号
制御バイトは0x00。

バイト数について、自身のbubble数を$n_1$、相手のbubble数を$n_2$とすると、
計$18 + 9n_1 + 9n_2$ Byte。
- $0$:
  - 制御バイト: i8
- $[1, 4]$:
  - 自身のangle: f32
- $5$:
  - 自身のスコア: i8
- $6$:
  - 自身のbubble数$n_1$: u8
- $[7,7 + 9n_1]$:
  - 自身のbubbleの情報が$n_1$個続く。
  - 各bubbleの情報は9Byteで、
    - 最初4Byteがx座標: f32
    - 次の4Byteがy座標: f32
    - 最後の1Byteが色: u8。赤なら0x00、白なら0x01。
- $[8 + 9n_1, 12 + 9n_1]$:
  - 相手のangle: f32
- $13 + 9n_1$:
  - 相手のスコア: i8
- $14 + 9n_1$:
  - 相手のbubble数$n_2$: u8
- $[15 + 9n_1, 15 + 9n_1 + 9n_2]$:
  - 相手のbubbleの情報が$n_2$個続く。
  - 各bubbleの情報は9Byteで、
    - 最初4Byteがx座標: f32
    - 次の4Byteがy座標: f32
    - 最後の1Byteが色: u8。赤なら0x00、白なら0x01。

## ゲーム開始信号
制御バイトは0x01。制御バイトのみ。

計1Byte。

## ゲーム終了信号
制御バイトは0x02。制御バイトのみ。

計1Byte。